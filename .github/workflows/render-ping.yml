name: Render Ping (Disabled)

# Render is no longer used in this project.
# The previous ping workflow is intentionally commented out for reference.
#
# on:
#   schedule:
#     - cron: '*/5 * * * *'
#   workflow_dispatch:
#
# jobs:
#   ping:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Ping Render
#         run: |
#           curl -fsS https://story-m7ow.onrender.com/api/health

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Render ping disabled
        run: echo "Render is not used. Workflow kept as commented reference."
